[package]
name = "fws_pipe_pump"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "fws_pipe_pump"
crate-type = ["cdylib"]

[dependencies]
libc = "0.2"
pyo3 = { version = "0.22", features = ["extension-module"] }
