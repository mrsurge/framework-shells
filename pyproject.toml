[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "framework_shells"
version = "0.0.54"
description = "Framework-agnostic process orchestration with PTY, pipe, and dtach backends."
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
  "fastapi",
  "aiofiles",
  "aiohttp>=3,<4",
  "pyyaml",
  "python-socketio>=5,<6",
]

[project.scripts]
fws = "framework_shells.cli.main:main"
fs = "framework_shells.cli.main:main"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["framework_shells*"]

[tool.setuptools.package-data]
framework_shells = ["py.typed", "*.pyi", "ui/*", "bin/*", "fws_pipe_pump*.so", "fws_pipe_pump*.pyd", "fws_pipe_pump*.dylib", "fws_pipe_pump*.dll"]
